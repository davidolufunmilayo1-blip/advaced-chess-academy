const {test}=require('node:test'),assert=require('node:assert/strict');const {createDiscovery}=require('../lib/discovery.cjs');
test('discovery uses a configured origin, unique page metadata, and keeps local sites out of the index',()=>{
 const local=createDiscovery();assert.match(local.robots(),/Disallow: \//);assert.equal(local.sitemap(),null);assert.match(local.html('<head><title>Old</title></head>','/'),/noindex/);
 const d=createDiscovery('https://club.example');const html=d.html('<head><title>Old</title><meta name="description" content="Old"><meta property="og:title" content="Old"></head>','/david.html');assert.match(html,/rel="canonical" href="https:\/\/club.example\/"/);assert.equal((html.match(/name="description"/g)||[]).length,1);assert.match(d.sitemap(),/https:\/\/club.example\/about.html/);assert.doesNotMatch(d.sitemap(),/localhost|api\/|#/);assert.match(d.robots(),/Sitemap: https:\/\/club.example\/sitemap.xml/);assert.match(d.html('<head><title>Old</title></head>','/chess-extras/watch.html'),/Watch chess &amp; follow live games/);assert.throws(()=>createDiscovery('https://user:secret@example.com'));assert.throws(()=>createDiscovery('https://example.com/path'));
});
